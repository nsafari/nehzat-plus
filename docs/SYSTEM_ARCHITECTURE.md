# مستندات جامع سیستم درس‌پلنر (Lesson Planner)

## فهرست
- [مقدمه و اهداف](#مقدمه-و-اهداف)
- [نقش‌ها و سطوح دسترسی](#نقش‌ها-و-سطوح-دسترسی)
- [داستان‌های کاربری](#داستان‌های-کاربری)
- [مدل داده](#مدل-داده)
- [معماری API](#معماری-api)
- [معماری فرانت‌اند](#معماری-فرانت‌اند)
- [فرآیندهای کاری](#فرآیندهای-کاری)
- [نیازمندی‌های غیرعملکردی](#نیازمندی‌های-غیرعملکردی)
- [نقشه راه توسعه](#نقشه-راه-توسعه)

---

## مقدمه و اهداف

سیستم **درس‌پلنر (Lesson Planner)** بستری برای مدیریت فرآیندهای آموزشی روزانه، پایش عملکرد متربیان، مدیریت تکالیف روزانه و ارسال فایل‌های صوتی و مستندات است. این سیستم برای مدارس و مؤسسات آموزشی طراحی شده و از معماری دو لایه (بک‌اند و فرانت‌اند) با قابلیت اجرا روی موبایل بهره می‌برد.

### اهداف اصلی

- **مدیریت تکالیف روزانه**: هر متربی تکالیف روزانه دریافت می‌کند و می‌تواند فایل ارسال کند
- **ضبط و ارسال صوت**: امکان ضبط صوت در مرورگر و ارسال به سرور
- **قانون گوش‌دادن اجباری**: متربی باید ۳ بار فایل آموزشی را گوش دهد تا ضبط فعال شود
- **نمودار پیشرفت**: نمایش پیشرفت دو هفته‌ای متربیان
- **مدیریت دوره‌ها**: ایجاد و مدیریت دوره‌ها، تکالیف و ضمیمه‌ها
- **گردش کار تأیید ثبت‌نام**: ثبت‌نام متربی و تأیید توسط مدیر
- **پشتیبانی موبایل**: اجرا روی اندروید با Capacitor
- **معماری API دوگانه**: قابلیت جابه‌جایی بین HTTP واقعی و Mock

---

## نقش‌ها و سطوح دسترسی

| نقش | سطح دسترسی | وضعیت |
|-----|-----------|--------|
| **متربی (Student)** | مشاهده تکالیف، ارسال فایل، ضبط صوت، پروفایل | پیاده‌سازی شده |
| **مدیر سیستم (Admin)** | مدیریت کاربران، دوره‌ها، تکالیف، ضمیمه‌ها، آمار | پیاده‌سازی شده |
| **استاد (Teacher)** | مدیریت تکالیف و نمرات (از طریق مدیر) | در برنامه آینده |
| **مربی (Coach)** | ثبت فعالیت‌های هفتگی | در برنامه آینده |
| **مسئول شعبه** | تأیید تغییر دسته‌بندی، ارزیابی مربیان | در برنامه آینده |
| **مسئول مالی** | ثبت تعهدات و پرداخت‌ها | در برنامه آینده |
| **والدین (Parent)** | مشاهده پیام‌ها و وضعیت مالی | در برنامه آینده |
| **ستاد** | دسترسی به آمار کل شعب | در برنامه آینده |

---

## داستان‌های کاربری

### متربی

#### ثبت‌نام
- فرم شامل: نام، نام‌خانوادگی، نام کاربری، ایمیل، شماره تماس (`09xxxxxxxxx`)، رمز عبور (حداقل ۸ کاراکتر)
- پس از ثبت‌نام پیام «در انتظار تأیید مدیر سیستم هستید» نمایش داده می‌شود
- کاربر به صفحه ورود هدایت می‌شود

#### ورود
- کاربران با وضعیت `pending`: پیام «حساب شما در انتظار تأیید است»
- کاربران با وضعیت `rejected`: «حساب شما رد شده است»
- ورود موفق: متربی به `/dashboard`، مدیر به `/admin` هدایت می‌شود
- احراز هویت مبتنی بر localStorage (token موقت)

#### مشاهده دوره‌های فعال
- در نوار کناری (sidebar) فقط دوره‌های با وضعیت `active` نمایش داده می‌شود

#### تایم‌لاین تکالیف
- محور افقی با دایره‌های رنگی: سبز (گذشته)، آبی (امروز)، خاکستری (آینده)
- کلیک روی دایره → مودال جزئیات تکلیف باز می‌شود

#### جزئیات تکلیف
- مودال شامل: عنوان، تاریخ، توضیحات، دستورالعمل، فایل‌های ضمیمه (قابل پخش/دانلود)، وضعیت تحویل
- نمایش نمره روزانه و تجمعی، بازخورد استاد

#### قانون ۳ بار گوش‌دادن (Listen Gate)
- تا وقتی متربی صدای آموزشی اصلی را **۳ بار کامل** گوش نکرده، دکمه ضبط غیرفعال است
- هر بار گوش‌دادن وقتی شمرده می‌شود که پخش به انتها برسد (یا حداقل ۹۵٪ پیشروی)
- UI شمارنده شفاف نمایش می‌دهد: `تعداد گوش‌دادن: X / 3`
- پس از رسیدن به `3/3`، ضبط برای همان تکلیف فعال می‌شود

#### ضبط و ارسال صدا
- دکمه ضبط فقط در مودال یک تکلیف فعال (پس از ۳ بار گوش‌دادن)
- `MediaRecorder` با نمایش «● در حال ضبط...»
- پس از توقف، پخش فایل ضبط‌شده امکان‌پذیر
- دکمه «ارسال صوت» فایل را با `FormData` (فیلد `audioFile`) به سرور می‌فرستد

#### تاریخچه ارسال‌ها
- کارت‌های مجزا شامل: عنوان تکلیف، تاریخ، نمره، وضعیت، پخش‌کننده صوت، بازخورد

#### پروفایل
- کلیک روی نام در هدر: مودال شامل نام، نام‌خانوادگی، ایمیل، تلفن، نقش، وضعیت حساب

#### خروج
- پاک کردن `localStorage` و هدایت به `/auth/login`

#### محدودیت‌های دسترسی
- نقش‌های غیرمتربی: مدیر به `/admin`، دیگران به صفحه ورود
- کاربران تأییدنشده (pending/rejected) امکان ورود ندارند
- عدم دسترسی به مسیر `/admin` برای متربیان

#### محدودیت فایل
- حداکثر ۱۰MB
- فرمت‌های مجاز: صوتی (mp3, wav, ogg, webm)، PDF، DOC/DOCX، تصویری (jpeg, png, gif, webp)، txt

### مدیر سیستم

#### مدیریت کاربران
- مشاهده لیست کاربران در انتظار تأیید (`GET /admin/users/pending`)
- تأیید کاربر: تکمیل اطلاعات متربی (نام، نام‌خانوادگی، ایمیل، تلفن، کد متربی) و انتخاب دوره
- رد کردن کاربر

#### مدیریت دوره‌ها
- ایجاد، ویرایش، حذف دوره‌ها
- جستجو و فیلتر دوره‌ها بر اساس وضعیت

#### مدیریت تکالیف
- ایجاد تکلیف با عنوان، تاریخ، توضیحات، دستورالعمل، حداکثر نمره
- ایجاد سری تکالیف روزانه (مثلاً ۱۲ روز متوالی)
- ویرایش و حذف تکالیف
- مدیریت ضمیمه‌ها (فایل صوتی، تصویری، مستندات)

#### آمار سیستم
- مشاهده آمار کل: تعداد دوره‌ها، تکالیف، ضمیمه‌ها، دوره‌های فعال
- گزارش دوره خاص

---

## مدل داده

### موجودیت‌های اصلی (۷ جدول)

#### User (`users`)

| فیلد | نوع | توضیحات |
|------|-----|---------|
| `Id` | int (PK) | شناسه یکتا |
| `Username` | varchar(100) | نام کاربری یکتا |
| `PasswordHash` | varchar(255) | هش رمز عبور (BCrypt) |
| `FirstName` | varchar(100) | نام |
| `LastName` | varchar(100) | نام خانوادگی |
| `Email` | varchar(255) | ایمیل |
| `PhoneNumber` | varchar(20) | شماره تماس |
| `ImageUrl` | varchar(500) | آدرس تصویر پروفایل |
| `StudentId` | int (FK → students) | ارجاع به متربی (اختیاری) |
| `ApprovalStatus` | enum | `pending` / `approved` / `rejected` |
| `UserType` | enum | `student` / `admin` |

#### Student (`students`)

| فیلد | نوع | توضیحات |
|------|-----|---------|
| `Id` | int (PK) | شناسه یکتا |
| `FirstName` | varchar(100) | نام |
| `LastName` | varchar(100) | نام خانوادگی |
| `Email` | varchar(255) | ایمیل (یکتا) |
| `StudentId` | varchar(20) | کد متربی (یکتا) |
| `PhoneNumber` | varchar(20) | شماره تماس |
| `Address` | varchar(500) | آدرس |
| `DateOfBirth` | datetime | تاریخ تولد |
| `Status` | varchar(20) | وضعیت (پیش‌فرض: `active`) |

روابط: یک `User` می‌تواند یک `Student` داشته باشد. یک `Student` در چند `StudentCourse` و `AssignmentSubmission` شرکت دارد.

#### Course (`courses`)

| فیلد | نوع | توضیحات |
|------|-----|---------|
| `Id` | int (PK) | شناسه یکتا |
| `Title` | varchar(200) | عنوان دوره |
| `Description` | text | توضیحات |
| `CourseCode` | varchar(50) | کد دوره |
| `Credits` | int | تعداد واحد |
| `Instructor` | varchar(200) | نام استاد |
| `Status` | varchar(20) | `active` / `inactive` |
| `StartDate` | datetime | تاریخ شروع |
| `EndDate` | datetime | تاریخ پایان |
| `MaxStudents` | int | حداکثر متربیان |

#### Assignment (`assignments`)

| فیلد | نوع | توضیحات |
|------|-----|---------|
| `Id` | int (PK) | شناسه یکتا |
| `CourseId` | int (FK) | ارجاع به دوره |
| `Title` | varchar(200) | عنوان |
| `Description` | text | توضیحات |
| `Type` | varchar(50) | نوع تکلیف |
| `MaxScore` | decimal | حداکثر نمره |
| `AssignmentDate` | date | تاریخ تکلیف (فیلد کلیدی برای روزانه) |
| `Status` | varchar(20) | `active` |
| `Instructions` | text | دستورالعمل (شامل فایل صوتی آموزشی) |

#### AssignmentAttachment (`assignment_attachments`)

| فیلد | نوع | توضیحات |
|------|-----|---------|
| `Id` | int (PK) | شناسه یکتا |
| `AssignmentId` | int (FK) | ارجاع به تکلیف (حذف آبشاری) |
| `Title` | varchar(200) | عنوان |
| `Description` | text | توضیحات |
| `Kind` | enum | `audio` / `image` / `document` / `text` / `other` |
| `Url` | varchar(500) | آدرس فایل |
| `DisplayOrder` | int | ترتیب نمایش |

#### AssignmentSubmission (`assignment_submissions`)

| فیلد | نوع | توضیحات |
|------|-----|---------|
| `Id` | int (PK) | شناسه یکتا |
| `StudentId` | int (FK) | ارجاع به متربی |
| `AssignmentId` | int (FK) | ارجاع به تکلیف |
| `SubmissionDate` | date | تاریخ ارسال |
| `Status` | enum | `pending` / `submitted` / `graded` / `late` |
| `DailyScore` | decimal | نمره روزانه |
| `CumulativeScore` | decimal | نمره تجمعی |
| `Feedback` | text | بازخورد استاد |
| `AudioFileUrl` | varchar(500) | فایل صوتی ارسالی |
| `DocumentUrl` | varchar(500) | فایل مستند |
| `Notes` | text | یادداشت |
| `IsCompleted` | bool | تکمیل شده |
| `TimeSpent` | int | زمان صرف‌شده (دقیقه) |

#### StudentCourse (`student_courses`) - جدول پیوند

| فیلد | نوع | توضیحات |
|------|-----|---------|
| `Id` | int (PK) | شناسه یکتا |
| `StudentId` | int (FK) | ارجاع به متربی |
| `CourseId` | int (FK) | ارجاع به دوره |
| `EnrollmentDate` | datetime | تاریخ ثبت‌نام |
| `Status` | varchar(20) | `enrolled` |
| `FinalGrade` | decimal | نمره نهایی |
| `LetterGrade` | varchar(2) | نمره حرفی |
| `Notes` | text | یادداشت |

### وضعیت‌های کاربر
- **pending**: در انتظار تأیید مدیر
- **approved**: تأیید شده و متربی ایجاد شده
- **rejected**: رد شده

---

## معماری API

### پیش‌نیازها
- تمام مسیرها با پیشوند `/` (بدون `/api/v1`)
- احراز هویت: localStorage با `dummy-token` (فعلاً middleware واقعی وجود ندارد)
- بک‌اند: ASP.NET Core 8+ روی پورت ۳۰۰۰ (توسعه) یا پیکربندی‌شده
- فرمت فایل: multipart/form-data برای آپلود

### احراز هویت

| روش | مسیر | توضیحات |
|-----|------|---------|
| POST | `/auth/signin` | ورود با username و password |
| POST | `/auth/signup` | ثبت‌نام کاربر جدید (multipart) |

### متربیان

| روش | مسیر | توضیحات |
|-----|------|---------|
| GET | `/students` | دریافت همه متربیان |
| GET | `/students/:id` | دریافت متربی با شناسه |
| GET | `/students/me/profile` | دریافت پروفایل کاربر فعلی |
| GET | `/students/:id/progress` | دریافت پیشرفت متربی |
| GET | `/students/:id/submissions` | دریافت ارسال‌ها (فیلتر با assignmentId) |
| POST | `/students/:id/assignments/:assignmentId/submit` | ارسال کار روزانه (با فایل صوتی) |
| POST | `/students/:id/submissions/:submissionId/upload` | آپلود فایل برای ارسال موجود |

### دوره‌ها

| روش | مسیر | توضیحات |
|-----|------|---------|
| GET | `/courses` | همه دوره‌ها |
| GET | `/courses/active` | دوره‌های فعال |
| GET | `/courses/:id` | جزئیات دوره |
| GET | `/courses/:id/assignments` | تکالیف دوره |
| POST | `/courses/:id/assignments` | ایجاد تکلیف |
| POST | `/courses/:id/assignments/daily-series` | ایجاد سری تکالیف روزانه |

### مدیریت (Admin)

| روش | مسیر | توضیحات |
|-----|------|---------|
| GET/POST | `/admin/courses` | مدیریت دوره‌ها |
| GET/PUT/DELETE | `/admin/courses/:id` | مدیریت دوره خاص |
| GET/POST | `/admin/courses/:courseId/assignments` | مدیریت تکالیف |
| PUT/DELETE | `/admin/assignments/:id` | مدیریت تکلیف خاص |
| POST | `/admin/courses/:courseId/assignments/daily-series` | ایجاد سری تکالیف |
| GET/POST | `/admin/assignments/:assignmentId/attachments` | مدیریت ضمیمه‌ها |
| POST | `/admin/attachments/:id/upload` | آپلود فایل ضمیمه |
| GET | `/admin/users/pending` | کاربران در انتظار تأیید |
| POST | `/admin/users/:userId/approve` | تأیید کاربر |
| POST | `/admin/users/:userId/reject` | رد کاربر |
| GET | `/admin/statistics` | آمار سیستم |

---

## معماری فرانت‌اند

### تکنولوژی‌ها
- **Angular 21** با کامپوننت‌های Standalone
- **TypeScript 5.9** (strict mode)
- **RxJS** برای مدیریت رویدادهای ناهمگام
- **Capacitor 8** برای اجرا روی اندروید
- **Vitest + jsdom** برای تست واحد
- **SCSS** با متغیرهای CSS سفارشی (`--lp-*`)
- **فونت وزیرمتن** برای متون فارسی
- **چیدمان RTL** (راست‌چین)

### ساختار مسیرها

| مسیر | کامپوننت | محافظ |
|------|---------|-------|
| `/auth/login` | Login | — |
| `/auth/register` | Register | — |
| `/dashboard` | Dashboard | AuthGuard |
| `/admin` | Admin Panel | AuthGuard + AdminGuard |

### معماری API لایه
- **`LessonPlannerApi`** — کلاس انتزاعی شامل همه متدهای API
- **`HttpLessonPlannerApi`** — پیاده‌سازی واقعی با HttpClient
- **`MockLessonPlannerApi`** — پیاده‌سازی درون‌مرورگری (in-memory) با داده‌های نمونه
- **`LESSON_PLANNER_API`** — Injection Token برای جابه‌جایی بین دو پیاده‌سازی
- انتخاب با `environment.useMockApi`

### بارگیری پویای API URL
- فایل `config.json` در زمان bootstrap برنامه بارگذاری می‌شود
- `window.__apiBase` به صورت پویا تنظیم می‌شود
- در صورت نبود فایل، برنامه همچنان کار می‌کند

### سرویس احراز هویت
- `AuthService` مبتنی بر `localStorage`
- ذخیره `dummy-token` و `CurrentUser` (JSON)
- `auth.interceptor.ts` هدر `Authorization: Bearer` را اضافه می‌کند

---

## فرآیندهای کاری

### ثبت‌نام و تأیید
1. کاربر فرم ثبت‌نام را پر می‌کند
2. کاربر با وضعیت `pending` ایجاد می‌شود
3. مدیر از داشبورد مدیریت، کاربران در انتظار تأیید را مشاهده می‌کند
4. مدیر اطلاعات متربی را تکمیل و کاربر را تأیید می‌کند
5. کاربر تأییدشده می‌تواند وارد سیستم شود

### گردش کار تکلیف روزانه
1. مدیر (یا استاد) تکلیف روزانه با تاریخ مشخص ایجاد می‌کند
2. متربی در داشبورد خود، تکلیف را در تایم‌لاین مشاهده می‌کند
3. متربی فایل صوتی آموزشی را گوش می‌دهد (۳ بار کامل)
4. پس از فعال شدن ضبط، صوت خود را ضبط و ارسال می‌کند
5. مدیر (یا استاد) نمره و بازخورد ثبت می‌کند
6. متربی نتیجه را در تاریخچه مشاهده می‌کند

### آپلود فایل
1. فایل با `FormData` و فیلد `audioFile` ارسال می‌شود
2. سرور فایل را با نام یکتا (UUID) ذخیره می‌کند
3. مسیر فایل در دیتابیس ثبت می‌شود
4. فایل از طریق `public/uploads/...` قابل دسترسی است

---

## نیازمندی‌های غیرعملکردی

| حوزه | وضعیت فعلی |
|------|-----------|
| **امنیت** | احراز هویت با `dummy-token` در localStorage (موقتی) |
| **CORS** | کاملاً باز (`AllowAnyOrigin`) |
| **RTL/فارسی** | پشتیبانی کامل با فونت وزیرمتن |
| **موبایل** | پشتیبانی اندروید با Capacitor 8 |
| **مدیریت خطا** | نمایش پیام خطا از `error.error.message` |
| **API دوگانه** | قابلیت جابه‌جایی HTTP/Mock بدون تغییر کامپوننت‌ها |
| **تست** | Vitest با jsdom |
| **شمارنده گوش‌دادن** | پایدار در حالت Mock (localStorage) |

---

## نقشه راه توسعه

| فاز | وضعیت | توضیحات |
|-----|-------|---------|
| **فاز ۰**: زیرساخت | ✅ تکمیل | ASP.NET Core + Angular + SQLite/SQL Server |
| **فاز ۱**: هسته متربی و مدیر | ✅ تکمیل | ثبت‌نام، ورود، تکالیف، ارسال صوت، تأیید کاربران |
| **فاز ۲**: قانون ۳ بار گوش‌دادن | ✅ تکمیل | Listen Gate + شمارنده + Mock پایدار |
| **فاز ۳**: معماری API دوگانه | ✅ تکمیل | Injection Token + HTTP/Mock + بارگیری پویای URL |
| **فاز ۴**: نمودار پیشرفت | 🔄 در حال توسعه | نمودار دو هفته‌ای پیشرفت متربی |
| **فاز ۵**: نقش استاد | 📅 برنامه آینده | مدیریت تکالیف، نمره‌دهی، بازخورد |
| **فاز ۶**: نقش‌های بیشتر | 📅 برنامه آینده | مربی، مسئول شعبه، مالی، والدین |
| **فاز ۷**: رقابت و لیگ | 📅 برنامه آینده | مسابقات، رتبه‌بندی، لیگ درسی |
| **فاز ۸**: بهینه‌سازی | 📅 برنامه آینده | i18n، دسترسی‌پذیری، حالت آفلاین |

---

## تکنولوژی‌های اصلی

### بک‌اند
- **.NET** (ASP.NET Core) با target framework `net10.0`
- **Entity Framework Core 8** با SQL Server (و SQLite برای توسعه)
- **BCrypt.Net-Next** برای هش کردن رمز عبور
- معماری: Controller → Service → Repository (با DbContext مستقیم)

### فرانت‌اند
- **Angular 21** (Standalone Components)
- **TypeScript 5.9**
- **Capacitor 8** (اندروید)
- **Vitest** (تست)
- **SCSS** با معماری CSS سفارشی

### پایگاه داده
- **توسعه**: SQLite (`lesson-planner.db`)
- **تولید**: SQL Server
- ایجاد خودکار جداول با `EnsureCreated()`
- دیتای نمونه در زمان راه‌اندازی (۳ متربی، ۲ دوره، ۳۶ تکلیف)

### CI/CD
- استقرار خودکار روی GitHub Pages با push به `main`
- GitHub Actions
- انتشار APK در Releases

---

## اطلاعات تماس و مخزن

- **مخزن**: https://github.com/mohammadj13891391-nehzat/nehzat-plus
- **نسخه فعلی**: v1.4.0
- **Web App**: https://mohammadj13891391-nehzat.github.io/nehzat-plus/
- **APK**: بخش Releases گیت‌هاب
