import { XpBadge } from '../../models/lesson-planner.models';
import { mockXpBadges } from '../mock-lesson-planner-data';

// ── XP badges (mutable, seeded) ──
export const initialXpBadges: XpBadge[] = [...mockXpBadges];
export const initialXpActivities: any[] = [];