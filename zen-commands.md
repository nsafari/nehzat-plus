---
description: اجرای دستی Pipeline چندمدله OpenCode Zen
---

این دستور Pipeline را برای درخواست جاری فعال می‌کند.

استفاده:
- `/pipeline` — اجرای کامل ۶ مرحله
- `/pipeline analyze` — فقط تحلیل (DeepSeek)
- `/pipeline plan` — برنامه‌ریزی (Nemotron)
- `/pipeline code` — پیاده‌سازی (North Mini Code)
- `/pipeline review` — بررسی کد (MiMo)
- `/pipeline verify` — تأیید نهایی (Big Pickle)

Pipeline خودکار در هر گفتگو فعال است. این دستور برای کنترل صریح است.